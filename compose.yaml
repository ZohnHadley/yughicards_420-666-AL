services:
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
  postgres:
    image: pgvector/pgvector:latest
    restart: always
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=yugy
      - POSTGRES_PASSWORD=muto
    ports:
      - "5433:5432"